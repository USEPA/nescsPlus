import { SingleQueryAction } from './single-query-action';

describe('SingleQueryAction', () => {
  it('should create an instance', () => {
    expect(new SingleQueryAction()).toBeTruthy();
  });
});
